namespace MathCarRaceUWP
{
	internal static class ZIndexValues
	{
		internal const int candidateGridPointLine = 100;   // on top of everything else

		internal const int raceLine = 50;	// on top of grid

		internal const int grid = 20;	// on top of track
		
		internal const int innerBorder = 10;    // on top of the track

		internal const int startingLine = 8;    // on top of the track

		internal const int outerBorder = 5;
	}
}
